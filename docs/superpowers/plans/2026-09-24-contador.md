# Contador com Limites e Step — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Componente React de contador com incrementar/decrementar/resetar, step configurável e limites mínimo/máximo com validação automática — Next.js em JavaScript, simples e auditável (trabalho de faculdade).

**Architecture:** Um único componente client-side (`components/Contador.js`) com quatro `useState`. Cada requisito do enunciado vira uma função nomeada com comentário curto em português. Validações no `onBlur` dos inputs, sem `useEffect`. Estilos em CSS Modules.

**Tech Stack:** Next.js (App Router, JavaScript), CSS Modules, npm, Git.

**Spec:** `docs/superpowers/specs/2026-09-24-contador-design.md`

**Contexto:** projeto criado do zero na pasta atual. O repo git já contém a spec em `docs/` (o `create-next-app` aceita pastas com `.git` e `docs` sem conflito). Todos os comandos abaixo rodam na raiz do projeto. Sistema: Windows/PowerShell.

---

### Task 1: Scaffold do Next.js

**Files:**
- Create: tudo que o `create-next-app` gera (`package.json`, `app/layout.js`, `app/page.js`, `app/globals.css`, configs, `.gitignore`)

- [ ] **Step 1: Rodar o create-next-app**

```bash
npx --yes create-next-app@latest . --js --eslint --app --no-tailwind --no-src-dir --import-alias "@/*" --yes
```

Notas: rodar na raiz (`react-contador`). Ele instala as dependências automaticamente — pode demorar vários minutos (use timeout generoso, ex.: 600000 ms). Não deve pedir nada interativamente (todos os flags + `--yes`).

- [ ] **Step 2: Conferir o resultado**

```bash
npm run build
```

Expected: build concluído sem erros (ex.: "Compiled successfully").

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: scaffold do Next.js com JavaScript"
```

---

### Task 2: Limpar os padrões do scaffold

**Files:**
- Modify: `app/layout.js`
- Modify: `app/globals.css`
- Delete: `app/page.module.css`, `public/*.svg`

- [ ] **Step 1: Sobrescrever `app/layout.js`** (remove fontes externas do scaffold — build mais simples, sem rede)

```js
import './globals.css';

// Layout raiz do Next.js (App Router).
export const metadata = {
  title: 'Contador',
  description: 'Exercício: contador com limites e step',
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **Step 2: Sobrescrever `app/globals.css`**

```css
/* Reset mínimo para o exercício */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Fonte do sistema (sem dependências externas) */
body {
  font-family: system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif;
  color: #222;
  background: #f5f5f5;
}

/* Inputs e legend herdam a fonte da página */
button,
input,
legend {
  font: inherit;
}
```

- [ ] **Step 3: Remover arquivos não usados**

```bash
Remove-Item -LiteralPath app/page.module.css
Get-ChildItem public -Filter *.svg | Remove-Item
```

(O `page.module.css` vira órfão quando a Task 3 substituir a página; os SVGs só eram usados pela página demo.)

- [ ] **Step 4: Conferir**

```bash
npm run build
```

Expected: build sem erros.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: layout e reset minimos, sem fontes externas"
```

---

### Task 3: Componente Contador (lógica + UI + estilos)

**Files:**
- Create: `components/Contador.js`
- Create: `components/Contador.module.css`
- Modify: `app/page.js`

- [ ] **Step 1: Criar `components/Contador.js`** (código completo do exercício)

```js
'use client';

// Componente client-side: usa estado (useState) e eventos do usuário.
import { useState } from 'react';
import styles from './Contador.module.css';

// Exercício: contador com step e limites (mínimo/máximo) configuráveis.
export default function Contador() {
  // Req. 1: o contador começa em 0.
  const [contador, setContador] = useState(0);

  // Req. 6: limites configuráveis pelo usuário (mínimo sempre <= máximo).
  const [minimo, setMinimo] = useState(0);
  const [maximo, setMaximo] = useState(10);

  // Req. 5: o step sempre deve ser maior que zero.
  const [step, setStep] = useState(1);

  // Req. 2: soma o step ajustando ao máximo (nunca passa do máximo).
  function incrementar() {
    setContador(Math.min(contador + step, maximo));
  }

  // Req. 3: subtrai o step ajustando ao mínimo (nunca fica abaixo dele).
  function decrementar() {
    setContador(Math.max(contador - step, minimo));
  }

  // Req. 4: reset volta para 0, para o mínimo se ele for maior que 0,
  // ou fica no máximo se todo o intervalo for negativo.
  function resetar() {
    setContador(Math.min(Math.max(0, minimo), maximo));
  }

  // Handlers de digitação: guardam o número digitado.
  // Campo vazio não muda o estado (deixa digitar "-5" com calma).
  function mudarStep(evento) {
    const texto = evento.target.value;
    if (texto !== '') setStep(Number(texto));
  }

  function mudarMinimo(evento) {
    const texto = evento.target.value;
    if (texto !== '') setMinimo(Number(texto));
  }

  function mudarMaximo(evento) {
    const texto = evento.target.value;
    if (texto !== '') setMaximo(Number(texto));
  }

  // Req. 5: ao sair do campo, step inválido (<= 0) volta para 1.
  function validarStep() {
    if (step <= 0) {
      setStep(1);
    }
  }

  // Req. 6: mínimo nunca passa do máximo.
  // Req. 7: contador é reajustado para dentro do novo intervalo.
  function validarMinimo() {
    const novoMinimo = Math.min(minimo, maximo);
    setMinimo(novoMinimo);
    setContador(Math.min(Math.max(contador, novoMinimo), maximo));
  }

  // Req. 6: máximo nunca fica abaixo do mínimo.
  // Req. 7: contador é reajustado para dentro do novo intervalo.
  function validarMaximo() {
    const novoMaximo = Math.max(maximo, minimo);
    setMaximo(novoMaximo);
    setContador(Math.min(Math.max(contador, minimo), novoMaximo));
  }

  return (
    <main className={styles.cartao}>
      <h1>Contador</h1>

      {/* Valor atual do contador em destaque */}
      <p className={styles.valor}>{contador}</p>

      <div className={styles.botoes}>
        {/* Botão desabilita quando o contador já está no mínimo */}
        <button onClick={decrementar} disabled={contador === minimo}>
          - Step
        </button>

        <button onClick={resetar}>Resetar</button>

        {/* Botão desabilita quando o contador já está no máximo */}
        <button onClick={incrementar} disabled={contador === maximo}>
          + Step
        </button>
      </div>

      {/* Req. 6: inputs que tornam o contador dinâmico e configurável */}
      <fieldset className={styles.configuracao}>
        <legend>Configuração</legend>

        <label>
          Step
          <input
            type="number"
            step="any"
            value={step}
            onChange={mudarStep}
            onBlur={validarStep}
          />
        </label>

        <label>
          Mínimo
          <input
            type="number"
            step="any"
            value={minimo}
            onChange={mudarMinimo}
            onBlur={validarMinimo}
          />
        </label>

        <label>
          Máximo
          <input
            type="number"
            step="any"
            value={maximo}
            onChange={mudarMaximo}
            onBlur={validarMaximo}
          />
        </label>
      </fieldset>
    </main>
  );
}
```

- [ ] **Step 2: Criar `components/Contador.module.css`**

```css
/* Cartão centralizado na tela */
.cartao {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 1rem;
  padding: 2rem;
  text-align: center;
}

/* Valor do contador em destaque */
.valor {
  font-size: 5rem;
  font-weight: bold;
  line-height: 1;
}

/* Linha de botões de ação */
.botoes {
  display: flex;
  gap: 0.5rem;
}

.botoes button {
  font-size: 1rem;
  padding: 0.5rem 1rem;
  border: 1px solid #999;
  border-radius: 6px;
  background: #fff;
  cursor: pointer;
}

.botoes button:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

/* Bloco de configuração (step e limites) */
.configuracao {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  padding: 1rem 1.5rem;
  min-width: 280px;
}

/* Cada linha: label à esquerda, input à direita */
.configuracao label {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 1rem;
}

.configuracao input {
  width: 90px;
  padding: 0.35rem 0.5rem;
  font-size: 1rem;
}
```

- [ ] **Step 3: Sobrescrever `app/page.js`**

```js
import Contador from '@/components/Contador';

// Página inicial: apenas mostra o contador do exercício.
export default function Home() {
  return <Contador />;
}
```

- [ ] **Step 4: Conferir**

```bash
npm run build
```

Expected: build sem erros.

- [ ] **Step 5: Smoke test**

```bash
npm run dev
```

Abrir http://localhost:3000 (ou `Invoke-WebRequest http://localhost:3000` — expect HTTP 200 e HTML contendo "Contador"). A página mostra 0, os três botões e o fieldset "Configuração". Interromper o servidor (Ctrl+C).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: componente Contador com step e limites validados"
```

---

### Task 4: README + verificação final

**Files:**
- Create: `README.md`

- [ ] **Step 1: Criar `README.md`**

````markdown
# Contador com limites e step

Exercício de React (Next.js em JavaScript): contador com step configurável e
limites mínimo/máximo definidos pelo próprio usuário, com validação automática.

Os inputs de mínimo e máximo tornam o contador dinâmico e configurável:
o usuário define o intervalo de valores sem precisar alterar o código.

## Como rodar

```bash
npm install
npm run dev
```

Abrir http://localhost:3000.

## Requisitos e onde estão no código (`components/Contador.js`)

| # | Requisito | Onde está |
|---|---|---|
| 1 | Contador começa em 0 | `useState(0)` de `contador` |
| 2 | Soma o step sem passar do máximo | `incrementar()` |
| 3 | Subtrai o step sem ir abaixo do mínimo | `decrementar()` |
| 4 | Reset para 0 (ou mínimo, se mínimo > 0) | `resetar()` |
| 5 | Step numérico > 0 | `validarStep()` |
| 6 | Limites configuráveis com mín ≤ máx | fieldset "Configuração" + `validarMinimo()` / `validarMaximo()` |
| 7 | Ajuste automático do contador fora do intervalo | reajuste em `validarMinimo()` / `validarMaximo()` |

As validações rodam no evento `onBlur` (quando o usuário sai do campo),
para não corrigir o valor no meio da digitação.

## Checklist de testes manuais

1. Abrir a página → contador mostra **0**
2. Step 1, máx 10: clicar `+` → 1, 2, 3...; clicar `-` → volta
3. Contador 8, step 5, clicar `+` → **10** (ajusta ao máximo, não passa)
4. Contador 2, step 5, clicar `-` → **0** (ajusta ao mínimo)
5. `Resetar` → **0**
6. Mín = 5, `Resetar` → **5** (mínimo > 0)
7. Step = 0 ou -3, sair do campo → volta para **1**
8. Mín = 20 (máx 10) → mínimo corrigido para **10**; contador reajustado
9. Máx = -5 (mín 0) → máximo corrigido para **0**; contador reajustado
10. Contador 8, mudar máx para 3 → contador vira **3** (ajuste automático)
````

- [ ] **Step 2: Build final**

```bash
npm run build
```

Expected: build sem erros.

- [ ] **Step 3: Checklist manual com o usuário**

Rodar `npm run dev`, abrir http://localhost:3000 e percorrer com o usuário os 10 itens da checklist do README. Todos devem passar.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "docs: readme com enunciado e checklist de testes"
```

---

## Notas de verificação por tarefa

| Tarefa | Verificação |
|---|---|
| 1 Scaffold | `npm run build` passa |
| 2 Limpeza | `npm run build` passa |
| 3 Componente | `npm run build` passa + página carrega com HTTP 200 |
| 4 README | `npm run build` passa + checklist manual (10 itens) com o usuário |
